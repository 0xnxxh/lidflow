# LidFlow v1.0.0

LidFlow 的首个正式版本，让 MacBook 桌面跟随盖子角度展开与收拢。

- Silk 柔光、Shade 深影、Frost 冰雾和 Ash 飞灰四种效果，分别保存参数。
- 实时铰链跟随、内置预览、可调清除角度与 60 / 120 fps 目标刷新。
- 关闭设置窗口后继续运行，Esc 恢复本次画面，睡眠和非活跃会话时暂停。
- 新的简约应用图标与自适应菜单栏图标。
- Sparkle 应用内更新：菜单栏手动检查，自动检查可在“连接与行为”关闭；更新包经 Ed25519 签名。

## 安装

需要 macOS 14+、Apple silicon Mac，以及可读取铰链传感器的 MacBook 才能使用真实开合效果。打开 DMG，将 LidFlow.app 拖到 Applications，然后启动。无兼容传感器时仍可使用内置样例。

本版本为 ad-hoc 签名，未使用 Apple Developer ID，也未经过 Apple 公证；首次打开可能被 Gatekeeper 拦截。确认来源后，可在系统设置“隐私与安全性”使用“仍要打开”。真实桌面效果需单独授予屏幕录制权限；画面只在内存处理，不录音、不保存或上传。

## 验证与边界

19 项核心测试、正式构建、应用签名、DMG 校验、更新源元数据与 Ed25519 签名验证。README 提供纯文字安装、功能和权限说明。

不同机型、HID 事件模式、真实睡眠/锁屏、多显示器切换、HDR 和全 Retina 捕获尚未完成全面硬件验收。首个发行包为 arm64；未提供 Intel 构建。
